export default function NotesLayout({ children }) {
  return (
    <div className="bg-gradient-to-b from-indigo-100 to-indigo-50 dark:from-gray-800 dark:to-gray-900 min-h-screen transition-colors">
      {children}
    </div>
  );
}
